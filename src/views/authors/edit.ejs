<% if (locals.validation_error!=undefined) { %> <%
  validation_error.forEach(element => { %>
    <div class="alert alert-danger" role="alert">
  <%=element.msg %>
</div>
  <% }) %> <% } %>
<h2 class="text-center">Yazar Düzenleme</h2>
<form class="col-lg-6 offset-lg-3" action="/authors/<%= author.id %>?_method=PUT" method="post">
   <div class="row justify-content-center">
  <input type="file" name="cover" class="filepond cover">
</div> 

<div>
  <label class="form-label">Ad:</label>
  <input class="form-control" type="text" name="firstname" value="<%= author.firstname %> " />
</div>
<div>
  <label  class="form-label">Soyad:</label>
  <input class="form-control" type="text" name="lastname" value="<%=author.lastname %> " />
</div>
<div>
  <label class="form-label" >Email:</label>
  <input class="form-control"type="email" name="email" value="<%= author.email %> " />
</div>
  <div>
    <label class="form-label" >Kullanıcı Adı:</label>
    <input class="form-control" type="text" name="username" value="<%= author.username %> " />
  </div>
  <div>
      <label class="form-label" for="cities">Şehirler</label>
      <select name="cities" class="form-select">
      <% cities.forEach(city => { %>
 <option label="<%=city.name%>" value="<%=city.name%>">
  <% }) %>
 </option>
</select>
    </div>
  <div class="text-end recipe-edit-container-settings">
  <div class="btn-group" role="group" aria-label="Basic example">
    <a   class="btn btn-danger rounded border-0" href="/authors/<%= author.id %> ">
      <i class="far fa-times-circle"></i> İptal</a
    İptal</a>
  <button class="btn btn-warning ms-2 border-0 text-white" type="submit">
    <i class="fas fa-redo"></i>
  Güncelle</button>
  </div>
  
  </div>
 
   </div>
   
    
</form>
